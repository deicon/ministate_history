name: ministate_history
description: | 
  Based on ministate State Management, this adds history including
  undo / redo feature to StateHolders

version: 0.0.2
homepage: https://github.com/deicon/ministate_history
repository: https://github.com/deicon/ministate_history

environment:
  sdk: ">=2.15.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  ministate: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  